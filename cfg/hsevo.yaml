defaults:
  - _self_
  - override hydra/output: local

hydra:
  job:
    chdir: True

# The chosen algorithm
algorithm: hsevo
problem: tsp_aco

# LLM parameters
model: openai/gpt-4o-mini-2024-07-18 # LLM model
temperature: 1.0
